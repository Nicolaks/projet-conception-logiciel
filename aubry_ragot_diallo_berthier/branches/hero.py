class hero:

    def __init__(self,grid):

        self.grid = grid

    def ajoutHero(grid): # Permet de mettre le joueur a sa place dans la grille.
        hauteurMap = h - 1
        centre = math.ceil(l/2) - 1 #placement du hero
        for i in range(h):
            for j in range(l):
                self.grid[hauteurMap][centre] = hero # ajout du hero dans le centre de la liste

#ajout déplacement

monhero = hero(grid)
monhero.ajoutHero(grid)
